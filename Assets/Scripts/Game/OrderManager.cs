using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class OrderManager : MonoBehaviour
{
    enum OrderState { Normal, GoAttack, SaveLife, }

    private void Start()
    {
        //ActiveToggles().FirstOrDefault();
    }
}
